// variables are where we store our data. it behaves like a box where we store our data
// how we create variablees and how we execute them

public class variables {
    public static void main(String[] args) {
        // the normal way of executing our code we can use the sout method
        System.out.println(3+4);// we have executed the data inside a print statement. print ln means it will print in another line
        // to avoid the above we can first store the data then execute
        int num1 = 24;// you have to specify the type of the value
        int num2 = 54;
        int result = num2+num1; // here we have use the add operator
        System.out.println("the result is equals to"+" "+result);
    }
}
